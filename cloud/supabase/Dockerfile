# Supabase local dev is handled via Docker Compose with official images.
# See docker-compose.yml and dev_container.sh.
#
# This Dockerfile is a custom postgres image for cases where you need
# to extend the database with additional extensions or seed data.

FROM supabase/postgres:15.1.1.78

# Add any custom extensions or seed SQL here:
# COPY ./seed.sql /docker-entrypoint-initdb.d/
